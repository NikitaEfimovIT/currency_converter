import {createTheme} from "@mui/material";

export const appTheme = createTheme({
	palette: {
		mode: 'light',
		primary: {
			main: '#4c2bad',
			contrastText: '#ECE7FD',
		},
		secondary: {
			main: '#ae94ef',
		},
	},
})
